import { style } from "@vanilla-extract/css";

export const page = style({
  height: "100dvh",
  width: "100%",
  display: "grid",
  placeItems: "center",
});

export const heading = style({
  fontSize: "2rem",
  fontWeight: "bold",
});
